const Hero = () => {
  return (
    <section className="flex flex-col justify-center text-left p-8 md:border-r">
      {/* <!-- Hero --> */}
      <h1 className="text-5xl font-bold md:text-6xl">A classroom for every child.</h1>
      <p className="">
          We fund the schools,
          train the teachers, and measure what works — so every child we reach today
          becomes a graduate tomorrow.
      </p>
    </section>
  );
}

export default Hero